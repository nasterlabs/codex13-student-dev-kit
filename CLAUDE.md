# Claude Instructions

Read `AGENTS.md` before making changes.

Use `task check` for non-installing verification and `task build` for installer
build verification on Windows. Preserve UTF-8 in NSIS files.
